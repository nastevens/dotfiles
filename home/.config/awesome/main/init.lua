require("main.error-handling")

return {
    config = require(... .. ".config"),
    layouts = require(... .. ".layouts"),
    layoutbox = require(... .. ".layoutbox"),
    menu = require(... .. ".menu"),
    runbox = require(... .. ".runbox"),
    taglist = require(... .. ".taglist"),
    tasklist = require(... .. ".tasklist"),
}
